# HTTP 测试规则
## 1. 比较运算符规则
- 使用 == 进行相等性比较，而不是 ===
- 使用 != 进行不相等比较，而不是 !==

## 2. 项目文件结构
### 测试相关文件
- cmd/test.http: HTTP 测试用例文件，包含所有 API 端点的测试逻辑
- cmd/http-client.env.json: HTTP 客户端环境变量配置文件，存储测试过程中的动态变量

### API 路由文件
- api/routes/routes.go: API 路由配置文件，定义了所有 HTTP 端点及其处理器映射

## 3. 测试用例编写规范
### 响应状态码验证
- 每个测试用例必须验证响应状态码
- 使用 client.assert() 进行断言
- 提供清晰的错误信息

### 环境变量处理
- 使用 client.global.set() 保存变量
- 使用 client.global.get() 获取变量
- 所有保存的变量需要添加日志输出

### 数据验证
- 验证响应体中的必需字段
- 验证字段类型和格式
- 验证数据的业务逻辑关系

### 错误处理
- 提供详细的错误信息
- 包含实际值和期望值的比较
- 使用模板字符串输出错误信息
- 在错误信息中包含相关的环境变量名称

## 4. 日志输出规范
- 使用 client.log() 输出关键信息
- 日志信息需要包含变量名和值
- 使用模板字符串格式化日志信息

## 5. 区块链特定规则
### 地址格式验证
- 必须以 "0x" 开头
- 长度必须为 42 个字符（包含 "0x"）
- 必须是有效的十六进制字符串

### 交易哈希验证
- 必须以 "0x" 开头
- 长度必须为 66 个字符（包含 "0x"）
- 必须是有效的十六进制字符串

### 区块号验证
- 必须是非负整数
- 支持特殊值：latest、earliest、pending
- 区块哈希必须符合交易哈希的格式要求

## 6. 测试用例组织
### 命名规范
- 使用清晰的描述性名称
- 包含测试目的和预期结果
- 使用中文注释说明测试逻辑

### 测试顺序
- 按照数据依赖关系组织测试用例
- 确保环境变量在使用前已正确设置
- 相关的测试用例应该组织在一起

## 7. 环境变量命名规范
- 使用下划线分隔的小写字母
- 名称应该反映变量的用途
- 相关变量使用相同的前缀

## 8. 断言规范
- 每个断言都需要有明确的错误信息
- 错误信息应该包含实际值和期望值
- 使用模板字符串格式化错误信息

## 9. 测试数据处理
- 验证数组和对象的类型
- 验证数组长度和对象属性
- 验证数据的格式和范围

## 10. 性能考虑
- 避免不必要的数据转换
- 优化循环和条件判断
- 合理使用变量缓存

## 11. TODO 注释规范
### AITODO 标记处理
- 使用 `AITODO:` 前缀标记需要 AI 处理的任务
- 在注释中清晰描述需要完成的任务
- 保持原有的代码注释格式

### AITODO 处理规则
- 识别并解析 `AITODO:` 标记的内容
- 根据任务描述生成相应的测试用例
- 遵循项目的测试规范和命名约定
- 确保生成的代码与现有代码风格一致

### AITODO 实现要求
- 保持与相邻测试用例的逻辑关联
- 继承已有的环境变量和测试数据
- 添加必要的验证和错误处理
- 包含适当的日志输出

### AITODO 注释格式
```http
### TODO: AITODO: 任务描述
### TODO: AITODO.1: 第一步任务描述
### TODO: AITODO.2: 第二步任务描述
```

### AITODO 代码修改范围
- 仅修改 `AITODO:` 标记的部分
- 不修改其他任何现有代码
- 保持其他代码的格式和注释不变
- 新代码必须与现有代码风格保持一致